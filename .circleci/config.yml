version: 2.1
jobs:
  download_python:
    docker:
      - image: circleci/ruby:2.6
    steps:
      - checkout
      - run:
          name: Install Python and dependencies to build page HTML
          command: |
           ls
workflows:
  version: 2
  default:
    jobs:
      - download_python           